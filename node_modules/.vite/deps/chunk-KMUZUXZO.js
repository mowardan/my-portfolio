import {
  FlowGraphExecutionBlockWithOutSignal
} from "./chunk-RVTG4NCI.js";
import {
  RichTypeAny
} from "./chunk-DFF4YCWU.js";
import {
  RegisterClass
} from "./chunk-S7NDMBDF.js";

// node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/Audio/flowGraphStopSoundBlock.js
var FlowGraphStopSoundBlock = class extends FlowGraphExecutionBlockWithOutSignal {
  /**
   * Constructs a new FlowGraphStopSoundBlock.
   * @param config - optional configuration for the block
   */
  constructor(config) {
    super(config);
    this.sound = this.registerDataInput("sound", RichTypeAny);
  }
  /**
   * @internal
   */
  _execute(context, _callingSignal) {
    const soundValue = this.sound.getValue(context);
    if (!soundValue) {
      this._reportError(context, "No sound provided");
      this.out._activateSignal(context);
      return;
    }
    soundValue.stop();
    this.out._activateSignal(context);
  }
  /**
   * @returns class name of the block.
   */
  getClassName() {
    return "FlowGraphStopSoundBlock";
  }
};
RegisterClass("FlowGraphStopSoundBlock", FlowGraphStopSoundBlock);

export {
  FlowGraphStopSoundBlock
};
//# sourceMappingURL=chunk-KMUZUXZO.js.map
