import {
  FlowGraphExecutionBlockWithOutSignal
} from "./chunk-RVTG4NCI.js";
import {
  RichTypeAny
} from "./chunk-DFF4YCWU.js";
import {
  RegisterClass
} from "./chunk-S7NDMBDF.js";

// node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/Audio/flowGraphPauseSoundBlock.js
var FlowGraphPauseSoundBlock = class extends FlowGraphExecutionBlockWithOutSignal {
  /**
   * Constructs a new FlowGraphPauseSoundBlock.
   * @param config - optional configuration for the block
   */
  constructor(config) {
    super(config);
    this.sound = this.registerDataInput("sound", RichTypeAny);
  }
  /**
   * @internal
   */
  _execute(context, _callingSignal) {
    const soundValue = this.sound.getValue(context);
    if (!soundValue) {
      this._reportError(context, "No sound provided");
      this.out._activateSignal(context);
      return;
    }
    if (soundValue.state === 5) {
      soundValue.resume();
    } else if (soundValue.state === 2 || soundValue.state === 3) {
      soundValue.pause();
    }
    this.out._activateSignal(context);
  }
  /**
   * @returns class name of the block.
   */
  getClassName() {
    return "FlowGraphPauseSoundBlock";
  }
};
RegisterClass("FlowGraphPauseSoundBlock", FlowGraphPauseSoundBlock);

export {
  FlowGraphPauseSoundBlock
};
//# sourceMappingURL=chunk-3LQV7LD7.js.map
