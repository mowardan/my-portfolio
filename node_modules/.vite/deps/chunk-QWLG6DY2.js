import {
  FlowGraphExecutionBlockWithOutSignal
} from "./chunk-RVTG4NCI.js";
import {
  RichTypeAny,
  RichTypeNumber
} from "./chunk-DFF4YCWU.js";
import {
  RegisterClass
} from "./chunk-S7NDMBDF.js";

// node_modules/@babylonjs/core/FlowGraph/Blocks/Execution/Audio/flowGraphSetSoundVolumeBlock.js
var FlowGraphSetSoundVolumeBlock = class extends FlowGraphExecutionBlockWithOutSignal {
  /**
   * Constructs a new FlowGraphSetSoundVolumeBlock.
   * @param config - optional configuration for the block
   */
  constructor(config) {
    super(config);
    this.sound = this.registerDataInput("sound", RichTypeAny);
    this.volume = this.registerDataInput("volume", RichTypeNumber, 1);
  }
  /**
   * @internal
   */
  _execute(context, _callingSignal) {
    const soundValue = this.sound.getValue(context);
    if (!soundValue) {
      this._reportError(context, "No sound provided");
      this.out._activateSignal(context);
      return;
    }
    const vol = this.volume.getValue(context);
    soundValue.volume = vol;
    this.out._activateSignal(context);
  }
  /**
   * @returns class name of the block.
   */
  getClassName() {
    return "FlowGraphSetSoundVolumeBlock";
  }
};
RegisterClass("FlowGraphSetSoundVolumeBlock", FlowGraphSetSoundVolumeBlock);

export {
  FlowGraphSetSoundVolumeBlock
};
//# sourceMappingURL=chunk-QWLG6DY2.js.map
